<div class="container mt-5">
  <h1>All pets</h1>

  <ul>
    <% @restaurants.each do |r| %>
      <li><%= link_to r.name, restaurant_path(r) %></li>
    <% end %>
  </ul>

  <%= link_to "Add a pet", new_restaurant_path %>
</div>
